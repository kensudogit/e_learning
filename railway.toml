# Railway / 類似 PaaS 向け
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Healthcheck 失敗でデプロイ全体が落ちると "Application not found" になるため無効化。
# アプリ側 /health は提供する。
restartPolicyType = "ON_FAILURE"
